<!--
Origin: Created by Codex lead agent on 2026-05-17.
Role: Planning/review notes only; not acceptance evidence for any gate.
-->

# Aily V1 Findings

Last updated: 2026-05-17

## Architecture Findings

- The current repo already includes partial V1 infrastructure: LangGraph
  dependencies, `aily/orchestration/`, `WorkflowRunStore`, SourceStore canonical
  Markdown records, a watched inbox service, and an optional
  `SourceFoundationGraph` path.
- The authoritative V1 plan still requires stronger service boundaries:
  `aily/main.py` remains orchestration-heavy and should be reduced to app wiring.
- `SourceFoundationGraph` currently wraps the whole DIKIWI foundation path inside
  `run_data`; later graph nodes mostly validate stage output. This does not yet
  satisfy the V1 side-effect idempotency and resume requirements.
- The `/api/ui/workflows/iwi` path is useful as an early trigger lane, but it
  does not yet implement the confirmed workflow-plan model required for V1.
- Legacy Reactor/Entrepreneur behavior still needs a deliberate wrap/quarantine
  decision before V1 release.

## Testing Findings

- Legacy test infrastructure has been removed for the V1 redesign.
- Current minimum readiness verification is import/build sanity plus real-path
  evidence when runtime behavior changes.
- `uv run python -m compileall -q aily scripts` is the reliable Tier 0 local
  check. Bare `python3` is not the project environment and misses dependencies.
- Mocked evidence may validate development scaffolding, but cannot close product
  acceptance gates.
- Completion evidence should use original PDFs from `/Users/luzi/aily_chaos/pdf`
  whenever a milestone touches ingestion, conversion, DIKIWI, graph, vault,
  export, or release readiness.
- A gate cannot pass from one evidence source. Each gate must reconcile
  Obsidian vault contents with runtime results, durable database records,
  workflow/checkpoint state, events/logs, and direct reviewer observations.
- Obsidian review is required even for gates that should not write to the vault;
  in those cases the review proves absence of accidental vault side effects.
- The lead agent cannot manually author or modify gate evidence. Evidence must
  include origin headers and be generated by the app, evidence runner, test
  harness, external service, DB query/export, or independent reviewer process.

## Unattended Development Findings

- Use one goal per milestone slice.
- Each unattended run must update `task_plan.md` and `progress.md`; otherwise
  later agents cannot tell whether work is complete, paused, or only partially
  verified.
- Real evidence should be stored under `~/.aily/runs/<run_id>/manifest.json` and
  referenced from `progress.md`.

## Environment Readiness Findings

Checked on 2026-05-17:

- `/Users/luzi/aily_chaos/pdf` contains 286 original PDFs, totaling 538 MB.
- `.env` exists.
- Kimi, DeepSeek, Tavily, and generic LLM keys are configured.
- Obsidian REST API key is configured without the literal `Bearer ` prefix.
- The local Obsidian REST service is not currently listening on
  `127.0.0.1:27123`, so REST behavior cannot be claimed until Obsidian and the
  Local REST API plugin are running.
- `orchestrator_enabled=true`, `orchestrator_shadow_mode=false`,
  `inbox_watcher_enabled=true`, `email_delivery_enabled=true`, and
  `dikiwi_foundation_only_ingestion=true` are configured for local gate runs.
- `~/Aily/Inbox` and `~/.aily/runs` exist. LangGraph checkpoint and workflow
  DB files are expected to appear during graph-backed evidence runs.
- `uv run python` provides Python 3.11 and project dependencies including
  LangGraph, Mistune, pdfplumber, and pdf2image.
- `docling` and `easyocr` are not installed in the project environment.
- `mineru` and `mineru-api` commands are available on PATH.
- Poppler command-line tools and Tesseract are not available on PATH.

## Subagent Findings

Initial read-only specialist team findings on 2026-05-17:

- Intake: watched inbox and SourceStore paths exist, but no acceptance-grade
  Gate 1 runner exists. The current source-foundation script is mocked and
  development-only.
- Intake: SourceStore source IDs are content-hash based for uploads, canonical
  Markdown persistence is real, and source jobs are durable SQLite rows.
- Intake: the V1 source graph currently uses simple `pdfplumber` extraction,
  not the richer legacy MinerU/Docling Chaos PDF path.
- Orchestration: SourceFoundationGraph is partially real, but `run_data` still
  executes Data/Information/Knowledge as one coarse DIKIWI block. Stage-level
  resume/idempotency is not yet Gate 2-ready.
- Orchestration: WorkflowRunStore lacks transition history and same-thread
  resume support for Gate 2.
- DIKIWI: primary V1 ingestion paths stop after Knowledge when foundation-only
  is enabled, but legacy incremental ingestion can still bypass this and should
  be quarantined or gated.
- DIKIWI: triggered I/W/I exists as an early backend lane, but it lacks workflow
  plan generation, confirmation pause, and pre-confirmation absence evidence.
- Vault/export: actual vault currently has `.obsidian` and `00-Chaos`; most
  numbered folders are absent until a writer/app setup path creates them.
- Vault/export: export and email dry-run modules are not implemented yet, so
  Gate 6 is a future feature slice.
- Security/config: Obsidian REST is reachable locally and configured without a
  literal `Bearer ` prefix. Gate 0 still needs machine-generated evidence and
  vault folder readiness.

## Aily-Copilot Product Research Findings

Reviewed on 2026-05-23:

- Obsidian Copilot's strongest reusable product patterns are layered context
  envelopes, deterministic vault search, inline citations, project-scoped
  context, tool metadata, memory separation, and preview-first file editing.
- Aily should not fork Obsidian Copilot as the main implementation because the
  repo is AGPL-3.0 and because Aily's backend workflows require durable stores,
  evidence gates, DIKIWI, Tavily research, dossier generation, and graph-quality
  scoring.
- The right architecture is an Aily FastAPI backend plus a thin Obsidian
  companion plugin.
- AC1 should begin with backend vault search/read/context APIs so the plugin can
  remain a UI shell rather than a second reasoning engine.

## Aily-Copilot Product Readiness Findings

Recorded on 2026-05-23:

- Aily-Copilot now has enough backend surface to behave like a real Obsidian
  companion: grounded chat, relevant-note navigation, project scopes, dossier
  generation, and preview-first writes.
- Preview-first writing is the key product safety guardrail. Creating a proposal
  stores a diff outside the vault; applying it is the only path that mutates a
  target note.
- Live provider evidence confirms `copilot.chat` uses DeepSeek and records
  sanitized traffic metadata with token usage.
- Real product value is now bottlenecked by vault content depth. The iCloud
  vault needs richer ingested PDFs/source notes before dossiers and graph
  recommendations can be judged at the level expected for leadership output.
